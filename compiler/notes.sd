
créer une chaine de caractere : 
Char my_string[] = "bonjour";

strcmp()



LINUX SYSTEM CALL : 
https://blog.rchapman.org/posts/Linux_System_Call_Table_for_x86_64/


GLIBC IMPLEMETATION : 

https://codebrowser.dev/glibc/glibc/string/strcat.c.html

-- remarque
la fonction printf ne va jamais call syscall(write) pour 2 raisons
    - printf va juste ecire dans le buffer stdout (ou autre), l'appel systeme write aura lieu uniquement lors d'un flush
    - la fonction la plus bas niveau dans la libc est __write, mais cette derniere est un wrapper autour de la fonction write qui est definis dans mon systeme directement (ellle est deja compilé sous forme de fichier .so). 
    Cette fonction est os specific donc elle depends de mon systeme d'exploitation
--



TABLEAU : 

Un tableau statique a une taille fixe connue à la compilation et est généralement alloué sur la pile (pour les tableaux locaux) ou dans la section de données (.data ou .bss) du programme (pour les tableaux globaux ou statiques).

